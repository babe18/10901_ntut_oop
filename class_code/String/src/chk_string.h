#ifndef CHK_STRING
#define CHK_STRING

#include <iostream>
#include <vector>
#include <string>
#include <cstring>
#include <stdexcept>

using namespace std;

void checkError(string input);
int checkAlpha(string input);
vector<int> checkUpper(string input);

#endif
